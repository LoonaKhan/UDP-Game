//
// Created by loona on 07/01/23.
//
#include "chunk.h"
#include <fmt/core.h>

int main() {
    fmt::print("size of chunk: {}", sizeof(chunk));

    char coords[] = {0,0},
            chunk_coords[] = {0,0},
            colour = 1,
            height=1;
    //char bin_data[] = block(coords, chunk_coords, colour, height);
}